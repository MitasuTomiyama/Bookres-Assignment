<h1>Editing Book</h1>
<%= form_for(@blog) do |f| %>

<h4>Title</h4>
<%= f.text_find :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>

<td><%= link_to "Show", show_book_path(@book) %></td>
<td><%= link_to "Back", index_book_path(@books) %></td>


<% end %>
